/** Public entry point for VG citation formatting and editing. */

export {
    buildCiteTemplate,
    buildCiteTemplateFromParts,
    parseCiteTemplate,
    sortCitationParams,
} from "./citation.ts";
